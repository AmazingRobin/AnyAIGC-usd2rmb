# 隐私政策

**插件名称**：AnyAIGC 汇率助手
**最后更新**：2026-09-08

## 简要说明

本插件**不收集、不存储、不上传任何个人信息**，也**不含任何统计、追踪或广告代码**。

## 详细说明

### 我们不收集任何数据

本插件没有服务器，不会向任何服务器发送数据。插件代码中不含任何网络请求
（`fetch`、`XMLHttpRequest`、`WebSocket`、图片打点等一律没有）。

具体而言，我们**不会**收集：

- 个人身份信息（姓名、邮箱、手机号、地址等）
- 健康信息
- 财务与支付信息
- 身份验证信息（密码、Cookie、令牌、API Key 等）
- 个人通信内容
- 位置信息
- 浏览历史
- 用户操作记录（点击、鼠标轨迹、按键等）
- 网站内容
- 上述之外的任何数据

### 本地存储的内容

插件会使用浏览器的扩展存储（`storage.local`）在**你自己的设备上**保存两项设置：

| 键名 | 含义 | 示例 |
| --- | --- | --- |
| `anyaigcPricingCurrency` | 当前选择的显示币种 | `"USD"` 或 `"RMB"` |
| `anyaigcPricingRate` | 你手动填写的美元兑人民币汇率 | `7.15` |

这两项数据：

- 仅保存在你的本地浏览器中，不会离开你的设备
- 不含任何个人信息
- 仅用于在你下次打开页面时恢复上次的选择
- 会随插件一并卸载删除

### 权限用途说明

插件仅申请一项权限：

- **`storage`**：用于在本地记住你选择的币种和填写的汇率。若不申请此权限，每次刷新页面都要重新设置一遍。

插件**没有**申请 `tabs`、`scripting`、`webRequest`、`cookies`、`history`、`downloads`
等任何敏感权限，也没有申请 `<all_urls>` 之类的宽泛主机权限。

### 作用范围

插件只在以下地址下运行，其他任何网站都不会加载本插件的代码：

- `https://anyaigc.ai/*`
- `https://www.anyaigc.ai/*`

在这些页面上，插件只做两件事：读取页面上已显示的美元价格文本，并把它替换成
换算后的人民币文本；以及插入一个币种切换面板。换算在你的浏览器内本地完成，
不涉及任何网络请求。插件不会修改页面向服务器发送的任何数据，也不会改变你的
实际账单金额 —— 它只改变屏幕上的显示。

### 数据共享与出售

我们没有任何数据，因此不存在共享、转让或出售数据的情况。我们不会将任何数据
用于与插件核心功能无关的用途，也不会用于判定信用状况或放贷。

### 变更

若未来版本涉及数据处理方式的变化，我们会更新本文件并提升插件版本号，在更新日志
中明确说明。

### 联系方式

如对隐私问题有疑问，请在项目的 GitHub 仓库中提交 Issue。
